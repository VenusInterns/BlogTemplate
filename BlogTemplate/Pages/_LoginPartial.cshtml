@using Microsoft.AspNetCore.Identity
@using BlogTemplate.Data
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <form asp-controller="Account" asp-action="Logout" method="post" id="logoutForm" class="navbar-right">

        <a asp-page="/Account/Manage/Index">Owner Settings</a>
        <button type="submit" class="btn btn-link navbar-btn navbar-link"><span class="glyphicon glyphicon-log-out"></span>Log out</button>
    </form>
}
else
{
    <span class="glyphicon glyphicon-log-in"></span><a asp-page="/Account/Login">Log in</a>
}
