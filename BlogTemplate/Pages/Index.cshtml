@page
@model IndexModel
@{
    ViewData["Title"] = "Home Page";

    foreach (var post in Model.Blog.PublicPosts)
    {
            <article>
                <h2><a href="/post/@post.Slug">@post.Title</a></h2>
                <p class="date">@post.PubDate.ToLocalTime().ToShortDateString()</p>
                <p>@post.Excerpt</p>
                <p class="commentCount"><a href="/post/@post.Slug">@post.Comments.Count</a></p>
            </article>
    }
}
